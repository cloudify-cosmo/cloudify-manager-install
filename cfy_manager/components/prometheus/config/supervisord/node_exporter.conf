[program:node_exporter]
user=cfyuser
group=cfyuser
command=/usr/local/bin/node_exporter --collector.supervisord --web.listen-address=127.0.0.1:{{ prometheus.node_exporter.metrics_port }}
environment=
    HOME="/etc/cloudify",
    USER="cfyuser"
